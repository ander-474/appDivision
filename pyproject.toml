[project]
name = "app-division"
version = "0.1.0"
description = "Aplicación para realizar divisiones"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flet[all]>=0.86.5",
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

